# Features

Use this directory for scoped product capabilities derived from requirements. A feature should describe a coherent outcome that can later be decomposed into stories.

Create one Markdown file per feature using `TEMPLATE.md`.
