# Validation

Use this directory for acceptance checks, evaluation plans, review evidence, and validation scenarios for PDLC artifacts.

Validation here is about proving that requirements, features, stories, plans, and delivered outcomes are coherent and complete. Executable product tests should live in root-level `tests/` once product implementation begins.

Create one Markdown file per validation scenario using `TEMPLATE.md`.
