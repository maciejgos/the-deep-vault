import type { RouteFixture } from "../game";

export const routeFixtures: Record<string, RouteFixture> = {
  "fixture.full-exposure-preview": {
    id: "fixture.full-exposure-preview",
    routeId: "full-exposure",
    choiceIds: ["choice.p1.begin-shift", "choice.a1.inspect-manifold", "choice.b17.open-feed"]
  }
};

