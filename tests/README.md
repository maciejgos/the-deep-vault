# Tests

This directory is reserved for executable product tests once implementation tooling exists.

Until product code is added under `src/`, use `spec/validation/` for PDLC artifact validation and acceptance evidence.
